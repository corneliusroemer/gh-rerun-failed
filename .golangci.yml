version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - misspell
    - unparam
    - prealloc

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true

run:
  timeout: 5m
